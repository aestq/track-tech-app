import type { Meta, StoryObj } from '@storybook/react'
import { AdminPageFilters } from './AdminPageFilters'

const meta: Meta<typeof AdminPageFilters> = {
  title: 'slice/AdminPageFilters',
  component: AdminPageFilters
}

type Story = StoryObj<typeof AdminPageFilters>

export const Primary: Story = {}
