import type { Meta, StoryObj } from '@storybook/react'
import { RoomForm } from './RoomForm'

const meta: Meta<typeof RoomForm> = {
  title: 'slice/RoomForm',
  component: RoomForm
}

type Story = StoryObj<typeof RoomForm>

export const Primary: Story = {}
